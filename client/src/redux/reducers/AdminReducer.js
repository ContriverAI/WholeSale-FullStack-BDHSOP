const initState = {
  adminToken: "",
  adminLoggedIn: false,
};
const AdminReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AdminReducer;
